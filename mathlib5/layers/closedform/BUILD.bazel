haskell_library(
    name = "closedform",
    srcs = glob(["src/**/*.hs"]),
    deps = [
        "//layers/sexpr:sexpr_ast",
        "@hackage//:sbv", # SMT-backed symbolic algebra
        "@hackage//:hermit", # GHC rewrite rules for equational reasoning
    ],
)

# Verified summation-to-closed-form rules (proved in Lean)
lean4_library(
    name = "summation_lemmas",
    srcs = glob(["lean/**/*.lean"]),
    deps = ["@lean4_mathlib//:Mathlib"],
)
