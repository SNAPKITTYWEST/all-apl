# End-to-end: APL source → TeX doc → Lean proof → LLVM binary → exec
sh_test(
    name = "e2e_pipeline_test",
    srcs = ["run_e2e.sh"],
    data = [
        "//layers/apl:apl_parser",
        "//layers/sexpr:sexpr_normalize",
        "//layers/liquid:lh_to_lean_bridge",
        "//layers/hol:mathlib5_core",
        "//layers/backend:llvm_backend",
        "//runtime/ffi:ffi_shim",
        "test_programs/simple_sum.apl",
    ],
    env = { "MATHLIB5_TEST_MODE": "1" },
)
