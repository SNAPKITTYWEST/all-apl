# CORE EXPERIMENT MAP

This document categorizes the various experiments and trees within the repository to provide clarity on their current status and intent.

---

## Primary Trees

- `mathlib5/`
- `agentos_source/`
- `mathlib5-ffi-bridge/`
- `mathrosetta_source/`

Reason:
- `mathlib5/` now contains the primary VSCP implementation (APL parser, S-Expr IR, Lean 4 bridge foundations, C-- Kernel).
- `agentos_source/`, `mathlib5-ffi-bridge/`, and `mathrosetta_source/` contain the bulk of the active source implementations and executable/proof surfaces.

---

## Secondary / Preserved Trees

- `legacy/`
- `snapkitty-shell/`
- `snapkitty-gitbucket/`

Reason:
- these trees are preserved for historical context, prior-art, or supporting shell/git infrastructure.

---

## Minimal / Non-authoritative Tree (DEPRECATED)

- `mathlib5/` (This classification is now outdated. `mathlib5/` has been repopulated).
